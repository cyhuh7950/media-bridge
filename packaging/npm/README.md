# Media Bridge CLI

이 패키지는 Media Bridge의 설치 후 실행·상태 확인·설정 진입점을 제공합니다.

```bash
npm install -g @bitkyc08/media-bridge
mb init
mb start
```

현재 실행기는 설치된 Media Bridge Python runtime을 사용합니다. 배포 패키지에
독립 실행 runtime을 포함하는 작업은 별도 패키징 단계에서 진행합니다.
