#!/bin/sh
set -eu
base_dir=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
export PYTHONPATH="$base_dir/opt/media-bridge/app${PYTHONPATH:+:$PYTHONPATH}"
exec "$base_dir/opt/media-bridge/runtime/bin/python" -m media_bridge_personal.web_entrypoint "$@"
